{# src/OC/PlatformBundle/Resources/views/Transport/editAdresse.html.twig #}

{% extends "PBTransportBundle::layout.html.twig" %}

{% block header %}
    <div id="header" class="well h2 text-center">
        {{ titre }}
    </div>
{% endblock %}

{% block pbtransport_body %}

    {{ include("PBTransportBundle:Transport:form.html.twig") }}
    {% if boutonDel == true %}
        <a href="{{ path('pb_transport_deletetransporteur', {'id': transporteur.id}) }}" class="btn btn-danger" onclick="return confirm('ATTENTION : êtes vous certain de vouloir supprimer cet transporteur ? Cette action est irrévocable.');">
            <i class="glyphicon glyphicon-trash"></i>
            Supprimer cette adresse
        </a>
    {% endif %}

{% endblock %}
